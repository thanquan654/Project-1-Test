server:
  port: 8080
  forward-headers-strategy: framework
app:
  fontend-url: "http://localhost:3000"
  ngrok:
    fontend-url-public: "https://real-unlikely-mastiff.ngrok-free.app"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_test
    username: root
    password: 123456
  jpa:
    hibernate:
      #ddl-auto: create-drop
      ddl-auto: update
    show-sql: true
    open-in-view: true